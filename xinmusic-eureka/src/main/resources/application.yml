server:
  port: 8081

eureka:
  instance:
    hostname: 127.0.0.1
  client:
    registerWithEureka: false #是否注册自己
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/